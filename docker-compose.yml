version: '3.8'

services:
  streamlit-app:
    build: .
    container_name: mesconsos
    env_file:
      - .env
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    restart: unless-stopped
